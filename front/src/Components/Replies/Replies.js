import Reply from "../Reply/Reply";
const Replies = () => {
  return (
    <div className="replies-container flex flex-col border-t border-gray-600">
      <Reply />
      <Reply />
    </div>
  );
};

export default Replies;
